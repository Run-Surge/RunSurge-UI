import DashboardPage from './DashboardPage';

export const metadata = {
  title: 'Dashboard - AuthApp',
  description: 'User dashboard for AuthApp',
};

export default function Dashboard() {
  return <DashboardPage />;
}