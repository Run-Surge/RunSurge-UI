import RegisterPage from './RegisterPage';

export const metadata = {
  title: 'Register - AuthApp',
  description: 'Create your AuthApp account',
};

export default function Register() {
  return <RegisterPage />;
}