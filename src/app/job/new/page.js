import CreateJobPage from "./CreateJobPage";

export const metadata = {
  title: "CreatJob - AuthApp",
  description: "Create Job for AuthApp",
};

export default function CreateJob() {
  return <CreateJobPage />;
}
